"use client";


const Page = () => {
//   const [isPending, startTransition] = useTransition();
//   const handleClick = () => {
//     startTransition(() => {
//       testAction();
//     });
//   };
  return (
    <div>
      {/* <Button disabled={isPending} onClick={handleClick}>
        Click
      </Button> */}
    </div>
  );
};

export default Page;
